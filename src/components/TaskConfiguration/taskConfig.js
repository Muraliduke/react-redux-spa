import React, {PropTypes} from 'react';

class taskConfig extends React.Component {
    render(){
        
        return(
            <div>
                Task Configuration
            </div>

        );

    }
}

export default taskConfig;